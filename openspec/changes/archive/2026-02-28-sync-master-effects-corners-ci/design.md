# Design: Sync Master Features into Specs & Docs

Documentation-only change — no source code modifications. Update specs and docs to match already-implemented features from master.

## Delta-Specs

- **components** — live sync, override preservation, child propagation, componentId mapping
- **scene-graph** — syncInstances, synced property list, child clone-with-mapping, independent corners
- **editor-ui** — Effects section, independent corner radius toggle, remove Create Instance button
- **desktop-app** — GitHub Actions CI/CD workflow
- **testing** — component-instance sync unit tests
